### Where-Used 图

打开 ABAP 对象 → 右键 → **可视化依赖关系图**
或：**Ctrl/Cmd+Shift+P** → **ABAP FS: Visualize Dependency Graph**

- **光标在符号上** → 谁使用那个方法/变量
- **光标在空白处** → 谁引用整个对象

缩放、平移、双击节点查看代码片段。

> ABAP 文件必须在编辑器中打开。
